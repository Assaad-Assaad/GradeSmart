#name: Building & Deploying the application
#
#on:
#  push:
#    branches: [ "master" ]
#  pull_request:
#    branches: [ "master" ]
#
#jobs:
#  maven-build:
#
#    runs-on: ubuntu-latest
#
#    services:
#      mysql:
#        image: mysql:8.2.0
#        ports:
#          - 3306:3306
#
#    steps:
#      - name: Checkout the code
#        uses: actions/checkout@v4
#
#      - name: Setup JDK 21
#        uses: actions/setup-java@v4
#        with:
#          java-version: '21'
#          distribution: 'corretto'
#          cache: maven
#
#      - name: Unit Tests
#        run: mvn test
#
#      - name: Build the application
#        run: |
#          mvn test
#          mvn package
#
#
#
#      - name: Build Docker Image
#        uses: docker/build-push-action@v6
#        with:
#            context: .
#            dockerfile: Dockerfile
#            push: false
#            tags: ${{ secrets.DOCKER_HUB_USERNAME }}/gradesmart:latest
#
#      - name: Login to Docker Hub
#        uses: docker/login-action@v3
#        with:
#              username: ${{ secrets.DOCKER_HUB_USERNAME }}
#              password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
#
#      - name: Push to Docker Hub
#        uses:  docker/build-push-action@v6
#        with:
#              context: .
#              dockerfile: Dockerfile
#              push: true
#              tags: ${{ secrets.DOCKER_HUB_USERNAME }}/gradesmart:latest
#
name: Building & Deploying the application

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  maven-build:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.2.0
        ports:
          - 3306:3306

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'corretto'
          cache: maven

      - name: Unit Tests
        run: mvn test

      - name: Build the application
        run: mvn package -DskipTests

      - name: Upload build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: gradesmart-jar
          path: target/*.jar

  docker-build:
    runs-on: ubuntu-latest
    needs: maven-build  # Ensures that maven-build is successful before running docker-build

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v3
        with:
          name: gradesmart-jar

      - name: Setup Docker
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          dockerfile: Dockerfile
          push: false
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/gradesmart:latest
          build-args: JAR_FILE=target/*.jar  # Assuming your Dockerfile is using this build argument

      - name: Push Docker Image to Docker Hub
        uses: docker/build-push-action@v6
        with:
          context: .
          dockerfile: Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/gradesmart:latest

